/**
 * Config sub-feature. Owns the per-card SDK options form (model, permission
 * mode, system-prompt-append, max-turns, additional directories) inside the
 * zoom view.
 */
export { SessionConfigPanel as ConfigTab } from "./SessionConfigPanel";
