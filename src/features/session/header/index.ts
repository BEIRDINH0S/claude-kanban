/**
 * Header sub-feature. Exports a single root component (`<ZoomHeader />`)
 * that owns the title/path/tags editor, the worktree status line, and the
 * toolbar (plan, stop, push, export, archive, close).
 */
export { ZoomHeader } from "./ZoomHeader";
